{{- if .Values.k8sScanner.enabled -}}
apiVersion: batch/v1
kind: CronJob
metadata:
  name: {{ include "deployguard-scanner.fullname" . }}-k8s
  namespace: {{ include "deployguard-scanner.namespace" . }}
  labels:
    {{- include "deployguard-scanner.labels" . | nindent 4 }}
    app.kubernetes.io/component: k8s-scanner
spec:
  schedule: {{ .Values.k8sScanner.schedule | quote }}
  concurrencyPolicy: {{ .Values.cronJob.concurrencyPolicy }}
  successfulJobsHistoryLimit: {{ .Values.cronJob.successfulJobsHistoryLimit }}
  failedJobsHistoryLimit: {{ .Values.cronJob.failedJobsHistoryLimit }}
  jobTemplate:
    spec:
      activeDeadlineSeconds: {{ .Values.cronJob.activeDeadlineSeconds }}
      backoffLimit: {{ .Values.cronJob.backoffLimit }}
      template:
        metadata:
          labels:
            {{- include "deployguard-scanner.selectorLabels" . | nindent 12 }}
            app.kubernetes.io/component: k8s-scanner
          {{- with .Values.pod.annotations }}
          annotations:
            {{- toYaml . | nindent 12 }}
          {{- end }}
        spec:
          serviceAccountName: {{ include "deployguard-scanner.serviceAccountName" . }}
          restartPolicy: OnFailure
          {{- with .Values.imagePullSecrets }}
          imagePullSecrets:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          containers:
            - name: scanner
              image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
              imagePullPolicy: {{ .Values.image.pullPolicy }}
              args:
                - scheduled
              env:
                # 필수: Helm values에서 주입
                - name: CLUSTER_ID
                  value: {{ include "deployguard-scanner.clusterId" . | quote }}
                - name: CLUSTER_NAME
                  value: {{ include "deployguard-scanner.clusterName" . | quote }}
                - name: API_URL
                  value: {{ include "deployguard-scanner.apiUrl" . | quote }}
                
                # 스캐너 타입
                - name: DG_SCANNER_TYPE
                  value: "k8s"
              envFrom:
                - configMapRef:
                    name: {{ include "deployguard-scanner.fullname" . }}-config
              resources:
                {{- toYaml .Values.k8sScanner.resources | nindent 16 }}
              securityContext:
                allowPrivilegeEscalation: false
                readOnlyRootFilesystem: true
                runAsNonRoot: true
                runAsUser: 1000
                capabilities:
                  drop:
                    - ALL
          {{- with .Values.pod.nodeSelector }}
          nodeSelector:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.pod.affinity }}
          affinity:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.pod.tolerations }}
          tolerations:
            {{- toYaml . | nindent 12 }}
          {{- end }}
{{- end }}